cmake_minimum_required(VERSION 2.8)
project( FeatureTracking )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( FeatureTracking FeatureTracking.cpp )
target_link_libraries( FeatureTracking ${OpenCV_LIBS} )
enable_testing()
