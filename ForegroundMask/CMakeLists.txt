cmake_minimum_required(VERSION 2.8)
project( ForegroundMask )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( ForegroundMask ForegroundMask.cpp)
target_link_libraries( ForegroundMask ${OpenCV_LIBS} )
enable_testing()
